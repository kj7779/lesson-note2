<form method="post" action="{{ path('app_make_lesson_delete', {'id': make_lesson.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ make_lesson.id) }}">
    <button class="btn">Delete</button>
</form>
