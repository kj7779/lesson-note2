{% extends 'base.html.twig' %}

{% block title %}Lesson4{% endblock %}

{% block body %}
    <h1>Lesson4</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lesson4.id }}</td>
            </tr>
            <tr>
                <th>Lesson_pose4</th>
                <td>{{ lesson4.lessonPose4 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lesson4_index') }}">back to list</a>

    <a href="{{ path('app_lesson4_edit', {'id': lesson4.id}) }}">edit</a>

    {{ include('lesson4/_delete_form.html.twig') }}
{% endblock %}
