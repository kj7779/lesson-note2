{% extends 'base.html.twig' %}

{% block title %}Lesson2{% endblock %}

{% block body %}
    <h1>Lesson2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lesson2.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lesson2_index') }}">back to list</a>

    <a href="{{ path('app_lesson2_edit', {'id': lesson2.id}) }}">edit</a>

    {{ include('lesson2/_delete_form.html.twig') }}
{% endblock %}
