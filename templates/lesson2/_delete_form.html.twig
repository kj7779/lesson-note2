<form method="post" action="{{ path('app_lesson2_delete', {'id': lesson2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lesson2.id) }}">
    <button class="btn">Delete</button>
</form>
