<form method="post" action="{{ path('app_lesson_note_delete', {'id': lesson_note.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lesson_note.id) }}">
    <button class="btn">Delete</button>
</form>
